#ifndef __DFS_ISMIN_HPP__
#define __DFS_ISMIN_HPP__

#include <dfs_code.hpp>
#include <types.hpp>

//bool dfs_code_is_min(types::DFSCode &dfs_code);
//bool dfs_code_is_min_internal(types::Projected &projected, types::DFSCode &dfs_code, types::DFSCode &min_dfs_code, types::Graph &graph_dfs_code);

#endif
